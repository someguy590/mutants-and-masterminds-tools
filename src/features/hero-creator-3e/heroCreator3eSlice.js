import { createSlice } from '@reduxjs/toolkit';

export const heroCreator3eSlice = createSlice({
    name: 'heroCreator3e',
    initialState: {
    },
    reducers: {
    }
});

export const {  } = heroCreator3eSlice.actions;

export default heroCreator3eSlice.reducer;

